# 构建上下文排除项（web/Dockerfile 的上下文是 web/ 目录）
#
# 不排除 node_modules / dist 会让 `COPY . .` 把本机产物带进镜像构建层：
# 既拖慢构建，又可能让旧产物覆盖镜像内新构建的结果。

node_modules
dist
.vite

# 本地环境文件（含后端域名等环境相关内容，不应进镜像）
.env
.env.local
.env.*.local

# 编辑器与系统文件
.DS_Store
*.log
