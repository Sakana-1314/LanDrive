# 前端环境变量示例
#
# 前端部署在公网，后端 API 部署在公司内网，两者不同源。把本文件复制为
# .env.production（生产）或 .env.local（本地临时）后修改。
#
# 也可以在构建命令前直接传环境变量，无需本文件：
#   HOST=https://ipip-filrs.local.19890605.xyz npm run build

# 后端域名（仅 origin，不要带 /api 与结尾斜杠）。
# 前端会据此拼出 `${HOST}/api` 作为接口地址。
# 留空则走同源 /api（适用于用反向代理把 /api 转发到内网的部署方式）。
# 示例：HOST=https://ipip-filrs.local.19890605.xyz
HOST=

# 接口连通性探测超时（毫秒）。超过该时间视为当前网络无法访问内网，
# 页面会提示「无法在此网络下使用，请更换网络再试！」
VITE_API_PROBE_TIMEOUT=6000
